/**
 * App-wide toast surface. Sonner renders through the branded <Toaster /> in
 * components/ui/sonner.tsx; import `toast` from here everywhere so styling and
 * behavior stay consistent (toast.success / toast.error / toast with options).
 */
export { toast } from 'sonner';
